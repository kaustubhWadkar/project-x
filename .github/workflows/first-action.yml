name: Test Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: echo "hello world"

      - name: print goodbye
        run: echo "done-bye"

  second-job:
    runs-on: ubuntu-latest
    steps:
      - name: create folder
        run: |
            echo " hello create foldr"
            echo " ok u can create"
            mkdir test-folder
            cd ./test-folder
            echo $(pwd)
        
  
  
    
